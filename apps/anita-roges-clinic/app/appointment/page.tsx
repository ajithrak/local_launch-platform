import { AppointmentPageContent } from '@locallaunch/healthcare-kit';
import { config } from '@/lib/config';

export default function AppointmentPage() {
  return <AppointmentPageContent config={config} />;
}
